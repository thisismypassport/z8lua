# A fork of samhocevar's z8lua with the intention of supporting p8 scripts that don't require graphics/sound/etc

In detail, includes:
* Various updates/fixes
* Api implementations that the original considered out of scope
* Tests

Also see z8lupa, which is a fork of lupa that integrates z8lua into python

